{
  # This is required, because the hm module is missing something
  # I may or may not be too lazy to fix it
  config.qt = {
    enable = true;
    platformTheme = "qt5ct";
    style = "kvantum";
  };
}
